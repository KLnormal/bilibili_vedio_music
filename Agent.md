# Agent.md — 项目要求与协作约定

> 本文件记录项目所有者对「Bilibili 视频内容爬取系统」的要求与协作约定。
> **任何参与本项目的 AI 助手 / 开发者，在开始任何改动前，都应先阅读本文件与
> `PROGRESS.md`**，确保理解需求边界、文档规范和当前进度。

---

## 1. 项目定位

一个运行在本地、以 Bilibili UP 主为管理单位的视频采集与下载系统，依据
`Bilibili 视频内容爬取系统——初代开发计划（Plan v0.1）` 实现。

一句话：**以 UP 主为组织单位、以 BV 号为唯一视频身份、以 SQLite 为本地状态中心、
以 btop 风格 TUI 为交互界面的增量视频采集与下载系统。**

---

## 2. 核心要求（来自项目所有者）

### 要求 1：子模块文档化 —— 每次更新必配 md

- 每个子模块（见 `bilibili_crawler/` 下的子包：`config` / `database` / `bilibili` /
  `crawler` / `filter` / `downloader` / `cli` / `state`）在**功能更新时**，都要
  同步生成或更新对应的 Markdown 文档。
- 目的：**后续任何一次更新，只要查询 md 文件就能快速上手**，无需翻源码理解现状。
- 文档统一放在 `docs/` 目录，命名与模块对应（如 `docs/database.md`、`docs/downloader.md`），
  并在 `docs/README.md`（索引）中登记。
- 每个模块 md 至少包含：
  1. 模块职责（一句话）；
  2. 对外接口 / 关键类与方法；
  3. 数据流或状态机（如有）；
  4. 最近一次更新的时间与变更摘要（Changelog）。
- 同时更新根目录 `PROGRESS.md` 的「模块状态」表。

### 要求 2：功能优先，暂不考虑边缘场景

- 优先**快速满足项目要求**，先跑通主流程。
- 不过度设计、不过度防御异常分支；能用简单实现就先简单实现。
- 边缘场景、极端输入、性能优化等，留到主功能稳定后再迭代。
- 判断标准：**先问「这是否阻碍核心需求落地」；不阻碍就暂缓。**

### 要求 3：良好的可维护性

- 职责严格分离：每个子模块只做一件事（沿用 Plan v0.1 的架构分层）。
- 命名清晰、有意义；关键逻辑加必要注释，尤其是「为什么这么写」。
- 数据库是唯一真实状态源；元数据与下载状态分离（见 Plan 第 18 节原则）。
- 核心逻辑与 TUI 解耦：关闭界面，爬虫 / 下载 / 数据库仍可独立运行。
- 改动尽量小、可回退；涉及行为的改动要跑一遍离线单测（`python -m unittest discover -s tests`）。

---

## 3. 文档组织约定

| 文件 | 作用 |
|------|------|
| `README.md` | 项目总览、安装、使用、配置、数据库设计、免责声明 |
| `Agent.md` | 本文件：项目要求与协作约定 |
| `PROGRESS.md` | 研发进度、模块状态、已修复问题、待办 |
| `docs/README.md` | 模块文档索引 |
| `docs/<module>.md` | 各子模块的详细文档 |

> 更新任何子模块时，**必须**同步更新对应的 `docs/<module>.md` 与 `PROGRESS.md`。

---

## 4. 协作工作流

1. 先读 `Agent.md` + `PROGRESS.md`，确认现状与待办。
2. 明确本次改动属于哪个子模块。
3. 改代码（遵循要求 2、3）。
4. 跑离线单测：`python -m unittest discover -s tests`。
5. 更新 `docs/<module>.md`（含 Changelog）与 `PROGRESS.md`。
6. 提交（提交信息说明「改了什么、为什么」），推送到 GitHub。

---

## 5. 已知的关键约定（坑位备忘）

- `config.yaml` / `test_config.yaml` 里的 `ffmpeg_path` 是**本机绝对路径**，不要提交进仓库。
- 账号密码 / SESSDATA 永不入库、不入配置、不入日志；仅持久化 Cookie 到 gitignored 的 `cookies.json`。
- Bilibili 空间接口有风控（`-352` / `-412`），需保留 WBI 签名 + 反爬参数 + 登录态。
- 下载清晰度由 `download.qn` + `download.ffmpeg_path` 共同决定（缺 ffmpeg 会退化为 720p）。
